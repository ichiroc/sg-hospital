<div class="bg-white shadow rounded-lg p-6 max-w-2xl mx-auto">
  <!-- 完了メッセージ -->
  <div class="text-center mb-8">
    <div class="inline-flex items-center justify-center w-16 h-16 bg-green-100 rounded-full mb-4">
      <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
      </svg>
    </div>
    <h1 class="text-3xl font-bold text-gray-900 mb-2">予約が完了しました</h1>
    <p class="text-gray-600">ご予約ありがとうございます</p>
  </div>

  <!-- 予約内容 -->
  <div class="bg-green-50 border border-green-200 rounded-lg p-6 mb-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">予約内容</h2>

    <dl class="space-y-3">
      <div class="flex border-b border-green-200 pb-3">
        <dt class="w-32 font-medium text-gray-700">予約番号</dt>
        <dd class="flex-1 text-gray-900"><%= @appointment.id %></dd>
      </div>

      <div class="flex border-b border-green-200 pb-3">
        <dt class="w-32 font-medium text-gray-700">予約日時</dt>
        <dd class="flex-1 text-gray-900">
          <%= l(@appointment.appointed_on, format: :long) %><br>
          <span class="text-xl font-semibold text-blue-600"><%= Appointment.appointed_slots[@appointment.appointed_slot] %></span>
        </dd>
      </div>

      <div class="flex border-b border-green-200 pb-3">
        <dt class="w-32 font-medium text-gray-700">お名前</dt>
        <dd class="flex-1 text-gray-900">
          <%= @appointment.name %><br>
          <span class="text-sm text-gray-600">(<%= @appointment.name_kana %>)</span>
        </dd>
      </div>

      <% if @appointment.patient_number.present? %>
        <div class="flex border-b border-green-200 pb-3">
          <dt class="w-32 font-medium text-gray-700">診察券番号</dt>
          <dd class="flex-1 text-gray-900"><%= @appointment.patient_number %></dd>
        </div>
      <% end %>

      <div class="flex border-b border-green-200 pb-3">
        <dt class="w-32 font-medium text-gray-700">電話番号</dt>
        <dd class="flex-1 text-gray-900"><%= @appointment.phone_number %></dd>
      </div>

      <div class="flex">
        <dt class="w-32 font-medium text-gray-700">診療目的</dt>
        <dd class="flex-1 text-gray-900">
          <%= Appointment::CONSULTATION_PURPOSES.find { |item| item[1] == @appointment.consultation_purpose }&.first %>
        </dd>
      </div>
    </dl>
  </div>

  <!-- 注意事項 -->
  <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
    <h3 class="font-semibold text-yellow-900 mb-2">ご来院時のお願い</h3>
    <ul class="list-disc list-inside text-sm text-yellow-800 space-y-1">
      <li>予約時間の10分前までにお越しください</li>
      <li>保険証をご持参ください</li>
      <li>診察券をお持ちの方はご持参ください</li>
      <li>体調不良の場合は事前にお電話ください</li>
    </ul>
  </div>

  <!-- アクションボタン -->
  <div class="flex gap-4">
    <%= link_to "トップページへ戻る",
        appointments_path,
        class: "flex-1 text-center px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition font-semibold" %>
  </div>
</div>
